# Archy de Berker

This repository now ships the site from `/Users/archydeberker/Documents/code/deberker-gatsby/next-site`.

Root-level npm scripts are thin wrappers around the Next.js app:

```bash
npm run dev
npm run build
npm run start
```

Netlify should build from the `next-site` base directory with Node 22.
